# Лабораторная работа: Тестирование программного обеспечения (Вариант 6)

Данный проект содержит реализацию двух приложений (исправного и содержащего ошибки), а также демонстрацию двух подходов к тестированию: метода **Черного ящика** и метода **Белого ящика (покрытие операторов)** с автоматическим запуском тестов реализованных посредством фреймворка `pytest` через GitHub Actions, настроен расчет покрытия кода (Code Coverage), настроен деплой отчетных таблиц на `GitHub-Pages` 

---

## Исходное задание

**Условие:** Пользователь вводит произвольную строку данных. Если введенная строка не содержит цифр, то необходимо вычислить, сколько раз в этой строке встречается символ «k» и символ «m». Если строка содержит цифры, то программа выводит `0`.

**Дополнительно:**
1. Создать второе приложение, внеся в исходный код две ошибки.
2. Разработать набор тестов методом черного ящика.
3. Разработать набор тестов методом белого ящика с покрытием операторов.
4. Сравнить эффективность методов по количеству найденных ошибок.

---
## Отчеты тестирования

👉 **[Кликните сюда, чтобы открыть Главную страницу отчетов тестирования](https://ilushka-obnimashka.github.io/software_design/)**

**Что доступно на сайте:**
1. **Покрытие кода (Coverage Report):** Интерактивная таблица, показывающая, какие строки кода приложения `app.py` и `app_with_errors.py` покрыты тестами, а какие остались без внимания.
2. **Результаты тестов (Pytest HTML):** Подробный лог выполнения каждого тест-кейса с подсветкой успешных и умышленно упавших проверок.

##  Реализация приложений

### 1. Исправное приложение (`app.py`)

```python
def process_string(data):
    # Проверяем наличие цифр во всей строке
    if any(char.isdigit() for char in data):
        return 0
    
    # Считаем символы 'k' и 'm'
    count_k = data.count('k')
    count_m = data.count('m')
    
    return {"k": count_k, "m": count_m}

if __name__ == "__main__":
    user_input = input("Введите строку: ")
    print(process_string(user_input))
```

### 2. Приложение с ошибками (`app_with_errors.py`)

```python
def process_string_buggy(data):
    # ERROR 1: Проверяется только первый символ, а не вся строка
    if len(data) > 0 and data[0].isdigit(): 
        return 0
    
    # ERROR 2: Опечатка в имени метода подсчета (.cont вместо .count)
    try:
        count_k = data.cont('k') 
        count_m = data.count('m')
        return {"k": count_k, "m": count_m}
    except AttributeError:
        return "System Error"

if __name__ == "__main__":
    user_input = input("Введите строку: ")
    print(process_string_buggy(user_input))
```

